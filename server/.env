# Required: set DATABASE_URL to your PostgreSQL connection string.
# With Docker: docker compose up -d (from repo root), then use the URL below.
# Replace USER and PASSWORD with the same credentials you use for: psql -h localhost -U USER -d landlord_portfolio
DATABASE_URL="postgresql://gaurav-agarwal@localhost:5432/landlord_portfolio?schema=public"
JWT_SECRET="your-super-secret-key-change-in-production"
JWT_EXPIRES_IN="7d"
PORT=3000
