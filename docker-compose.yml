services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: landlord
      POSTGRES_PASSWORD: landlord
      POSTGRES_DB: landlord_portfolio
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
